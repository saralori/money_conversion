nelmio_api_doc:
    documentation:
        info:
            title: Money Conversion
            description: This app provides a set of REST APIs which perform the basic arithmetic operations for old British currency.
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
